<h1>
    <%= @book.title %> 
</h1>

<p>
    <%= @book.author %> 
</p>

<%= link_to "Delete This Book", book_path(@book), data: {turbo_method: :delete, turbo_confirm: "Are You Sure ?"}, class: "btn btn-danger" %> 

<%= link_to "Home Page", root_path, class: "btn btn-secondary" %> 

<%= link_to "Edit Book", edit_book_path(@book), class: "btn btn-warning" %> 